{
  "name": "@adbl/cartridge",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "build": "npm run build:client && npm run build:server",
    "build:client": "vite build --outDir dist/client",
    "build:server": "vite build --ssr index.js --outDir dist/server",
    "preview": "cross-env NODE_ENV=production node dist/server"
  },
  "bin": {
    "cartridge": "index.js"
  },
  "dependencies": {
    "@adbl/bullet": "file:adbl-bullet-0.0.27.tgz",
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "happy-dom": "^15.7.3",
    "cross-env": "^7.0.3",
    "sirv": "^2.0.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "vite": "^5.2.10"
  }
}
